<div>
  <div class="name">{{first_name}} {{last_name}}</div>
  <div class="username">{{username}}</div>
  <div class="highscore">
    {{#each scores as |score|}}
      {{high_score}}
    {{/each}}
  </div>
</div>